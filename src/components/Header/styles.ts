import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#222',
    paddingBottom: 20,
    paddingTop: 28,
    fontWeight: 'bold',
    position: 'relative',
  },
  title: {
    fontSize: 32,
    color: '#fff',
    textAlign: 'center',
  },
  subtitle: {
    color: '#fff',
    textAlign: 'center',
  },
})
